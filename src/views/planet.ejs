<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/reset.css" />
    <link rel="stylesheet" href="/css/main.css" />
    <title><%= title %></title>
    <style>
      .planet {
        margin: var(--space) auto 0 auto;
      }
      /* Planet Container */
      .planet-container {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: var(--space);
      }
      /* Image Container Image */
      .planet-container > *:first-child {
        width: 100%;
        flex: 3;

        display: flex;
        align-items: center;
        justify-content: center;
      }
      /* Planet Container Information */
      .planet-container > *:last-child {
        flex: 2;
      }
      .planet-image {
        width: 100%;
      }
      /* Planet Description */
      .planet-description-container {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      .planet-description {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      /* Planet Information */
      .planet-info {
        flex: 1;

        margin: var(--space) 0 0 0;

        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
      }
      .planet-info > * {
        flex: 1;
      }
      .planet-tabs {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      .planet-image-container {
        position: relative;
      }
      .planet-image-container > *:nth-child(2) {
        position: absolute;
        width: 8rem;

        bottom: 0;
        transform: translate(0, 20%);
      }
      @media only screen and (max-width: 768px) {
        .planet-container {
          flex-direction: column;
        }
        .planet-image {
          width: 50%;
        }
        .planet-description-container {
          flex-direction: row;
        }
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <main>
      <div class="planet container">
        <div class="planet-container">
          <!-- Planet Image -->
          <div class="planet-image-container">
            <% if (!type || type === 'overview') { %>
            <img
              src="<%= planet.planet_image_path %>"
              alt="<%= planet.name %>"
              class="planet-image"
            />
            <% } %> <% if (type === 'internal') { %>
            <img
              src="<%= planet.internal_image_path %>"
              alt="<%= planet.name %>"
              class="planet-image"
            />
            <% } %> <% if (type === 'geology') { %>
            <img
              src="<%= planet.planet_image_path %>"
              alt="<%= planet.name %>"
              class="planet-image"
            />
            <img
              src="<%= planet.geology_image_path %>"
              alt="<%= planet.name %>"
              class="planet-image"
            />
            <% } %>
          </div>
          <!-- Planet Description -->
          <div class="planet-description-container">
            <div class="planet-description">
              <!-- Planet Content -->
              <h1><%= planet.name %></h1>
              <% if (!type || type === 'overview') { %>
              <p><%= planet.overview_content %></p>
              <% } %> <% if (type === 'internal') { %>
              <p><%= planet.structure_content %></p>
              <% } %> <% if (type === 'geology') { %>
              <p><%= planet.geology_content %></p>
              <% } %>
              <!-- Planet Source -->
              <% if (!type || type === 'overview') { %>
              <span
                >Source:
                <a href="<%= planet.overview_source %>" target="_blank"
                  >Wikipedia
                  <img src="/assets/icon-source.svg" alt="Planet Source" /></a
              ></span>
              <% } %> <% if (type === 'internal') { %>
              <span
                >Source:
                <a href="<%= planet.structure_source %>" target="_blank"
                  >Wikipedia
                  <img src="/assets/icon-source.svg" alt="Planet Source" /></a
              ></span>
              <% } %> <% if (type === 'geology') { %>
              <span
                >Source:
                <a href="<%= planet.geology_source %>" target="_blank"
                  >Wikipedia
                  <img src="/assets/icon-source.svg" alt="Planet Source" /></a
              ></span>
              <% } %>
            </div>
            <div class="planet-tabs">
              <a
                href="/planets/<%= planet.name.toLowerCase() %>/?type=overview"
              >
                <button>Overview</button>
              </a>
              <a
                href="/planets/<%= planet.name.toLowerCase() %>/?type=internal"
              >
                <button>Internal Structure</button>
              </a>
              <a href="/planets/<%= planet.name.toLowerCase() %>/?type=geology">
                <button>Surface Geology</button>
              </a>
            </div>
          </div>
        </div>
        <!-- Planet Information -->
        <div class="planet-info">
          <%- include('./partials/card', { header: "Rotation Time", content:
          planet.rotation_period}) %> <%- include('./partials/card', { header:
          "Revolution Time", content: planet.revolution_period}) %> <%-
          include('./partials/card', { header: "Radius", content:
          planet.radius_km}) %> <%- include('./partials/card', { header:
          "Average Temp.", content: planet.temperature_celsius}) %>
        </div>
      </div>
    </main>
  </body>
</html>
